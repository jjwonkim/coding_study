const get = (url, options) => {
    /* COMPRESS */ return Promise.resolve(new Response()); /* END */
};
const post = (url, options) => {
    /* COMPRESS */ return Promise.resolve(new Response()); /* END */
};
export default {};
