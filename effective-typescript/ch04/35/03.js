function calculateBoundingBox(f) {
    let box = null;
    const helper = (coords) => {
        // ...
    };
    const { geometry } = f;
    if (geometry) {
        helper(geometry.coordinates);
    }
    return box;
}
export default {};
